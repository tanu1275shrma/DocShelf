<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />

    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen pastel-gradient">
      <div class="form px-10 py-10 text-white">
        <form action="/create" method="post" class="flex flex-col items-center">
          <input
            name="title"
            class="block w-80 rounded-md outline-none px-25 py-4 bg-white text-black"
            type="text"
            placeholder="title goes here"
          />
          <textarea
            name="details"
            class="block w-[35rem] rounded-md outline-none mt-3 px-50 py-4 resize-none bg-white text-black"
            placeholder="write your task details"
          ></textarea>
          <input
            class="mt-5 bg-purple-400 px-5 py-3 rounded-md text-bold cursor-pointer"
            type="submit"
            value="Create Task"
          />
        </form>
      </div>
      <div class="tasks flex gap-8 flex-wrap p-10">
        <% if(files.length > 0 ) {%> <% files.forEach(function(val){ %>

        <div
          class="task min-w-72 px-3 py-6 rounded-md bg-white text-black flex flex-col justify-between"
        >
          <h1 class="text-black text-bold text-xl tracking-tighter">
            <%= val %>
          </h1>
          <div class="flex justify-between items-center pt-10 mt-auto">
            <a class="text-purple-500 inline-block" href="/file/<%= val %>"
              >Read More</a
            >
            <a class="text-black" href="/edit/<%= val %>">Edit Filename </a>
          </div>
        </div>
        <% }) %> <% } else { %>
        <h3 class="text-black">No tasks yet.</h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
